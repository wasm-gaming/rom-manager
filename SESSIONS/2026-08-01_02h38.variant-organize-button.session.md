# 2026-08-01 02:38 - Add Organize Button to Non-Consolidated Variant Files

## Summary
Add an organize button (`button.tree-organize`) to the right of `.btn-inline` for variant files in `RomDetails` that are not yet consolidated into canonical file names/paths.

## Planned Changes

1. **`src/core/rom-organize.ts`**:
   - Export `targetOf` and `isConsolidated` helper functions to calculate canonical target paths for cartridge and disc ROMs.

2. **`src/core/wizard-tree.ts`**:
   - Update `WizardFile` interface to include `isConsolidated?: boolean`.
   - Update `buildWizardTree` to accept optional `media?: 'cartridge' | 'disc'` parameter and compute `isConsolidated` for each local file in `WizardFile`.

3. **`src/components/RomDetails.tsx`**:
   - Add `onOrganize?: (path: string) => void` prop to `RomDetailsProps`, `GameView`, and `VariantRow`.
   - Import `OrganizeIcon` from `./icons`.
   - Render `<button class="tree-organize" ...>` next to `.btn-inline` in `VariantRow` when `file.isConsolidated === false` and `onOrganize` is present.

4. **`src/views/ROMExplorer.tsx`**:
   - Pass `media` to `buildWizardTree` when grouping folders.
   - Pass `onOrganize={handleOrganize}` to `<RomDetails />`.

5. **`src/styles/index.css`**:
   - Add/adjust styles for `.variant-file .tree-organize` to align cleanly right beside `.btn-inline`.

## Verification Plan
- Run existing test suites (`npm test`).
- Add/update unit tests in `wizard-tree.test.ts` to test `isConsolidated` flag computation.
- Verify UI rendering and layout.

## Walkthrough

### What was done:
- Exported `targetOf` in `src/core/rom-organize.ts` and added `isConsolidatedPath`.
- Updated `WizardFile` interface in `src/core/wizard-tree.ts` to include `isConsolidated?: boolean`.
- Updated `buildWizardTree` to calculate `isConsolidated` for each file on disk.
- Updated `RomDetails.tsx` to render `<button class="tree-organize">` with `<OrganizeIcon />` next to `.btn-inline` for non-consolidated variant files.
- Passed system `media` type to `buildWizardTree` and `onOrganize={handleOrganize}` to `<RomDetails />` in `ROMExplorer.tsx`.
- Updated `.variant-file` styling in `index.css`.
- Added unit test in `wizard-tree.test.ts` verifying `isConsolidated` computation. All 327 unit tests pass.

### Suggested 1-line commit message:
`feat: add organize button next to non-consolidated variant files`

