# Session: Select Added Game After Intake / Drop

Date: 2026-08-01 01:35

## Objective
Automatically select the newly added game / ROM files in the UI and details pane after an intake or drop operation completes.

## Context & Root Cause
In `src/views/ROMExplorer.tsx`:
- `handleIntakeConfirm` and `handleDropConfirm` apply the file copy operations, clear grouping caches, and bump `refreshToken`.
- However, neither function updates `selection` or calls `handleSelectionChange`.
- As a result, after adding a game, the view remains on its previous selection (or no selection), requiring the user to search for and select the newly added game manually.

## Proposed Changes

1. **`src/views/ROMExplorer.tsx`**:
   - In `handleIntakeConfirm`, collect the target paths of placed items (`intake.filter(isPlaced)`). Upon completion, call `setGameFile(undefined)` and `void handleSelectionChange(addedPaths)`.
   - In `handleDropConfirm`, collect target paths of intake items (`taken.filter(isPlaced)`) or copied files (`files`). Upon completion, call `setGameFile(undefined)` and `void handleSelectionChange(addedPaths)`.

2. **Verification**:
   - Run unit tests (`npx vitest run`) to verify all tests pass cleanly.
   - Confirm selection state updates when adding games/ROMs.

## Walkthrough

- **`src/views/ROMExplorer.tsx`**:
  - Updated `handleIntakeConfirm` and `handleDropConfirm` to extract the destination paths of placed/copied files.
  - Added logic invoking `setGameFile(undefined)` and `handleSelectionChange(addedPaths)` right after intake/drop confirmation, automatically selecting the added game/ROM files in the tree and details pane.
- **Verification**:
  - Ran `npx vitest run`; all 321 tests across 19 test files passed cleanly.

Suggested Commit Message:
`fix: automatically select newly added game/ROM files after intake confirmation`

Status: Completed

---

## Extension: Fixed Aspect-Ratio Wrapper for ROM Covers

Date: 2026-08-01 01:39

### Objective
Wrap `.rom-info-cover` in a container element (`.rom-info-cover-wrapper`) with a fixed `aspect-ratio` to avoid content layout shift (CLS) when loading cover images in the details pane.

### Proposed Changes

1. **`src/components/RomDetails.tsx`**:
   - Wrap `.rom-info-cover` inside a `<div class="rom-info-cover-wrapper">` container in `CoverFigure`.

2. **`src/styles/index.css`**:
   - Add `.rom-info-cover-wrapper` with `width: 180px; aspect-ratio: 3 / 4; flex-shrink: 0; border-radius: 6px; overflow: hidden;`.
   - Update `.rom-info-cover` to use `width: 100%; height: 100%; object-fit: cover;`.
   - Update `.rom-info-cover.placeholder` to fill 100% height and width of the wrapper.

### Verification Plan
- Run `npx vitest run` to ensure unit test suite passes.

### Walkthrough

- **`src/styles/index.css`**:
  - Added `.rom-info-cover-wrapper` with `width: 180px`, `aspect-ratio: 3 / 4`, `border-radius: 6px`, and `overflow: hidden`.
  - Updated `.rom-info-cover` and `.rom-info-cover.placeholder` to fill 100% of wrapper width/height to reserve 180x240px space during loading.
- **`src/components/RomDetails.tsx`**:
  - Wrapped cover image and placeholder elements inside `.rom-info-cover-wrapper` in `CoverFigure`.
- **Verification**:
  - Ran `npx vitest run`; all 321 unit tests passed cleanly.

Suggested Commit Message:
`style: wrap rom cover in fixed aspect ratio element to prevent layout shift`

Status: Completed

---

## Extension: System-Specific Aspect Ratio for ROM Cover Images

Date: 2026-08-01 01:47

### Objective
Update `.rom-info-cover-wrapper` aspect ratio to respect each gaming system's boxart aspect ratio (e.g., 1:1 for Game Boy/PS1/DS, 4:3 for GBA/N64/SNES, 3:4 for NES/Genesis/Mega Drive), and dynamically update to match the image's exact natural aspect ratio upon loading.

### Proposed Changes

1. **`src/core/rom-covers.ts`**:
   - Add `systemAspectRatio(system?: string): string` helper function mapping system names to expected aspect ratio strings (`'1 / 1'`, `'4 / 3'`, or default `'3 / 4'`).

2. **`src/core/rom-covers.test.ts`**:
   - Add unit tests for `systemAspectRatio` verifying correct ratio mappings for various retro console system names.

3. **`src/components/RomDetails.tsx`**:
   - Pass `system` prop to `CoverFigure`.
   - Compute initial aspect ratio from `systemAspectRatio(system)`.
   - Update aspect ratio dynamically on `<img>` `onLoad` (`img.naturalWidth / img.naturalHeight`).
   - Apply `style={{ aspectRatio }}` on `.rom-info-cover-wrapper`.

### Verification Plan
- Run `npx vitest run` to verify unit tests pass.

### Walkthrough

- **`src/core/rom-covers.ts`**:
  - Implemented `systemAspectRatio(system?: string): string` to return `'1 / 1'` for square boxart systems (GB, GBC, PS1, DS, 3DS), `'4 / 3'` for horizontal boxart systems (GBA, N64, SNES), and default `'3 / 4'` for vertical boxart systems.
- **`src/core/rom-covers.test.ts`**:
  - Added unit tests covering system aspect ratio resolutions across square, horizontal, and vertical console systems.
- **`src/components/RomDetails.tsx`**:
  - Updated `CoverFigure` component to accept `system` parameter and apply `style={{ aspectRatio: currentRatio }}` on `.rom-info-cover-wrapper`.
  - Added `onLoad` handler on `<img>` element to set `naturalRatio` to exact `img.naturalWidth / img.naturalHeight` upon load.
  - Passed `systemOf(...)` from `RomFileView`, `RomInfoView`, and `GameView` call sites.
- **Verification**:
  - Ran `npx vitest run`; all 324 unit tests across 19 test files passed cleanly.

Suggested Commit Message:
`style: adjust cover aspect ratio by gaming system and image natural dimensions`

Status: Completed

---

## Extension: Darken `.details-pane` Background in Light Mode

Date: 2026-08-01 01:52

### Objective
Darken the background color of `.details-pane` in light mode (e.g. using `var(--surface-3)` / `#f0f0f0` or `var(--surface-2)` / `#f5f5f5`) to provide better visual contrast against white surface cards in the details view.

### Proposed Changes

1. **`src/styles/index.css`**:
   - Update `.details-pane` background in light mode to `var(--surface-3)` (or `var(--surface-2)`), preserving `var(--app-bg)` in dark mode.

### Verification Plan
- Run `npx vitest run` to ensure tests pass cleanly.

### Walkthrough

- **`src/styles/index.css`**:
  - Set `.details-pane` background-color to `var(--surface-3)` (`#f0f0f0`) in light mode for better contrast against white cards.
  - Retained `var(--app-bg)` background-color for `:root[data-theme='dark'] .details-pane`.
- **Verification**:
  - Executed `npx vitest run`; all 324 unit tests passed cleanly.

Suggested Commit Message:
`style: darken details pane background in light mode`

Status: Completed



