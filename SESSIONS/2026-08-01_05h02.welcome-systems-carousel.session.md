# Session: Welcome Systems Carousel

**Date:** 2026-08-01 05:02  
**Topic:** Convert `.welcome-systems` into a 3-row infinite right-to-left marquee carousel with distinct speeds, prominent middle row, full page width, and removed background/borders.

## Planned Changes

1. **System Grouping & Data Structure (`WelcomePanel.tsx`)**:
   - Organize supported systems into 3 distinct rows:
     - **Top Row**: Handhelds and secondary systems (`GAMEBOY`, `GBC`, `GameGear`, `SMS`, `TGFX16`, `Atari2600`, `WonderSwan`, `PokemonMini`).
     - **Middle Row (Most Relevant)**: Flagship consoles (`NES`, `SNES`, `N64`, `GBA`, `MegaDrive`, `PSX`, `Saturn`, `NEOGEO`).
     - **Bottom Row**: Disc add-ons and classic hardware (`MegaCD`, `S32X`, `TGFX16-CD`, `NeoGeo-CD`, `ATARI5200`, `ATARI7800`, `AtariLynx`, `WonderSwanColor`).
   - Duplicate each row's system list in render to enable seamless 0% to -50% infinite CSS marquee scrolling.

2. **Markup Restructuring (`WelcomePanel.tsx`)**:
   - Structure `.welcome-systems` container with full-width container styling.
   - Render 3 row containers (`.welcome-systems-row`), each wrapping a scrolling track (`.welcome-systems-track`).

3. **Styling & Animation (`src/styles/index.css`)**:
   - Remove `border` and `background` from `.welcome-system` and `.welcome-system-logo`.
   - Expand `.welcome-systems` wrapper to span full page width (`width: 100vw; margin-left: calc(-50vw + 50%);` or breaking out of section bounds cleanly with full width `overflow: hidden`).
   - Apply continuous right-to-left marquee animation `@keyframes welcome-marquee-left` moving from `translateX(0)` to `translateX(-50%)`.
- Set different scroll speeds for each row (e.g. 36s for top, 24s for middle, 42s for bottom).
   - Style the middle row items (`.welcome-systems-row--middle .welcome-system`) with increased height, larger logos, and larger font sizes.

## Walkthrough

- Split system catalogue into three rows in `WelcomePanel.tsx`:
  - **Top row**: Handhelds and secondary systems.
  - **Middle row**: Primary flagship systems (`NES`, `SNES`, `N64`, `GBA`, `MegaDrive`, `PSX`, `Saturn`, `NEOGEO`).
  - **Bottom row**: Disc add-ons and classic consoles.
- Rendered duplicated system tracks per row (`[...systems, ...systems]`) with `@keyframes welcome-marquee-left` scrolling smoothly right-to-left in an infinite CSS loop.
- Adjusted speeds per row: `36s` (top), `24s` (middle - faster), and `44s` (bottom).
- Expanded carousel container to 100% viewport width with subtle edge gradient fading.
- Scaled up middle row height, logos (`3.25rem`), text size (`1.2rem`), and spacing for visual prominence.
- Increased default logo size (`2.35rem`) and text size (`0.95rem`) for top and bottom rows.
- Reduced scrolling speed across all rows (`52s` top, `38s` middle, `62s` bottom).
- Removed backgrounds and borders from `.welcome-system` and `.welcome-system-logo`.

**Suggested Commit Message:**
`feat(welcome): convert system logos display to 3-row infinite marquee carousel`

