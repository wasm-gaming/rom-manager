# 2026-08-01 05:09 - Prevent Active Cover on Missing Variants

## Summary
Ensure that variants with status `missing` (`variant.status === 'missing'`) are not marked with `.active-cover` class, even if their region matches the displayed cover region.

## Planned Changes

1. **`src/components/RomDetails.tsx`**:
   - Update `isCoverActive` in `VariantRow` to require `variant.status !== 'missing'`.

## Verification Plan

- Run unit test suite `npm test`.
- Verify in `RomDetails` component that missing variants do not receive `.active-cover` class styling.

## Walkthrough

### What was done:
- Updated `isCoverActive` in `VariantRow` (`src/components/RomDetails.tsx`) to check `variant.status !== 'missing'`.
- Verified that missing variants under `.variants-missing` no longer receive the `.active-cover` class even when region matches.
- Ran all 348 unit tests successfully (`npm test`).

### Suggested 1-line commit message:
`fix: prevent active cover class on missing variants`

