# Session: Refactor UI Components & Split ROMExplorer Sub-Views

**Date:** 2026-08-01 05:31  
**Topic:** Component Modularization & Architecture Refactoring

## Objectives
1. Make all components in `src/components/` 100% portable and reusable by ensuring they **only** import from `src/components/*` (using `@/components/...` alias) and external libraries, removing all dependencies on `src/core`, `src/services`, `src/types`, etc.
2. Refactor `src/components/FileTree.tsx` to contain only core basic logic and move its specific sub-components and types into `src/components/FileTree/*`.
3. Move domain-specific view components currently in `src/components/` (`RomDetails`, `MetadataEditor`, `SystemDetails`, `OrganizePanel`, `WelcomePanel`, `MediaDropModal`, `RomIntakeModal`, `PreferencesModal`) to `src/views/ROMExplorer/*`.
4. Split `src/views/ROMExplorer.tsx` into clean sub-views under `src/views/ROMExplorer/*` (e.g., `Header.tsx`, `RomDetails.tsx`, `WelcomePanel.tsx`, etc.).
5. Use `@` as alias for `src` across imported paths.

## Planned Changes

### 1. `src/components/` (Pure Reusable UI Components)
- **`src/components/BrandLogo.tsx`**: Keep pure UI SVG logo, update imports to `@/components/...`.
- **`src/components/SystemLogos.tsx`**: Keep pure UI system logos, update imports to `@/components/...`.
- **`src/components/WelcomeCardIllustrations.tsx`**: Keep pure UI SVG illustrations, update imports to `@/components/...`.
- **`src/components/icons.tsx`**: Remove import from `../core/file-types`. Inline icon mapping/mime helpers so `icons.tsx` is completely self-contained.
- **`src/components/Tabs.tsx`**: Remove import from `../services/I18nService`. Make tab titles/labels portable props.
- **`src/components/FileTree.tsx` & `src/components/FileTree/*`**:
  - `src/components/FileTree/types.ts`: Define component-level interfaces (`FileTreeNodeData`, `WizardGameData`, `MatchStatusType`, etc.) without importing domain modules.
  - `src/components/FileTree/utils.ts`: Pure helper functions for path parsing, system name extraction, and filtering.
  - `src/components/FileTree/FileTreeNode.tsx`: Tree item component for files and folders.
  - `src/components/FileTree/FileTreeHeader.tsx`: Toolbar / header for tree actions.
  - `src/components/FileTree/FileTreeContextMenu.tsx`: Right-click / item context actions.
  - `src/components/FileTree.tsx`: Main export combining the sub-components.

### 2. `src/views/ROMExplorer/*` (Domain Views & Sub-Views)
Move domain components that use project services/core into `src/views/ROMExplorer/`:
- `src/views/ROMExplorer/Header.tsx` (Extracted header bar)
- `src/views/ROMExplorer/WelcomePanel.tsx`
- `src/views/ROMExplorer/RomDetails.tsx`
- `src/views/ROMExplorer/SystemDetails.tsx`
- `src/views/ROMExplorer/MetadataEditor.tsx`
- `src/views/ROMExplorer/OrganizePanel.tsx`
- `src/views/ROMExplorer/PreferencesModal.tsx`
- `src/views/ROMExplorer/MediaDropModal.tsx`
- `src/views/ROMExplorer/RomIntakeModal.tsx`

### 3. `src/views/ROMExplorer.tsx`
- Update `ROMExplorer.tsx` to import sub-views from `@/views/ROMExplorer/*` and portable components from `@/components/*`.
- Simplify state management and sub-view orchestration.

## Status
Completed

## Walkthrough

1. **Portable UI Components in `src/components/`**:
   - Decoupled `icons.tsx` from `src/core/file-types` by implementing self-contained extension matching helpers.
   - Decoupled `Tabs.tsx` from `src/services/I18nService` by converting hardcoded translation calls into portable optional label props with sensible defaults.
   - Simplified `src/components/FileTree.tsx` into a lightweight core component and created modular subcomponents and types under `src/components/FileTree/*` (`types.ts`, `utils.ts`, `FileTreeNode.tsx`, `FileTreeHeader.tsx`, `RowIcon.tsx`).
   - Verified that **all** files in `src/components/` only import from other `src/components/` modules or standard external libraries.

2. **Domain Views Isolation in `src/views/ROMExplorer/*`**:
   - Extracted and moved domain-specific view components from `src/components/` to `src/views/ROMExplorer/`:
     - `src/views/ROMExplorer/WelcomePanel.tsx`
     - `src/views/ROMExplorer/RomDetails.tsx`
     - `src/views/ROMExplorer/SystemDetails.tsx`
     - `src/views/ROMExplorer/MetadataEditor.tsx`
     - `src/views/ROMExplorer/OrganizePanel.tsx`
     - `src/views/ROMExplorer/PreferencesModal.tsx`
     - `src/views/ROMExplorer/MediaDropModal.tsx`
     - `src/views/ROMExplorer/RomIntakeModal.tsx`
   - Created `src/views/ROMExplorer/Header.tsx` to encapsulate the top bar (title, logo, tabs, region buttons, preferences trigger).
   - Removed old domain component files from `src/components/`.

3. **`ROMExplorer.tsx` Refactoring & `@/` Alias**:
   - Refactored `src/views/ROMExplorer.tsx` to consume sub-views from `@/views/ROMExplorer/*` and portable components from `@/components/*`.
   - Replaced relative paths with `@/` path alias across modified components and main app files.

4. **Verification**:
   - Ran `npx tsc --noEmit` with zero errors.
   - Ran `npm test` with 348 passed tests across 21 test files.
   - Ran `npm run build` with clean production output.

### Suggested Commit Message
```text
refactor(ui): modularize portable components in src/components and split ROMExplorer sub-views
```
