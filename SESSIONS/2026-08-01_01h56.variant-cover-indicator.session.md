# Session: Variant Cover Indicator

Date: 2026-08-01 01:56

## Objective
Add a visual indicator (such as a blue right border) to `.variant` rows in the details view to indicate which variant corresponds to the currently displayed game cover.

## Proposed Changes
1. **`src/components/RomDetails.tsx`**:
   - Pass the active `cover` (`CoverShown`) prop down to `VariantRow` inside `GameView`.
   - Compute `isCoverActive` for each variant row (true when `cover?.region` is present and `variant.regions.includes(cover.region)`).
   - Apply the class `active-cover` to `.variant` when `isCoverActive` is true.

2. **`src/styles/index.css`**:
   - Add `.variant.active-cover` styling with a right border indicator (`border-right: 3px solid var(--accent)`).

## Walkthrough

Date: 2026-08-01 02:00

### Implemented Changes
- **`src/components/RomDetails.tsx`**: Updated `VariantRow` and `GameView` so the `cover` prop (`CoverShown`) is passed to each `VariantRow`. Calculated `isCoverActive` based on matching `cover.region` against `variant.regions`. Added `active-cover` class to `.variant`.
- **`src/styles/index.css`**: Added `.variant.active-cover` rule with a 3px right border styled in `var(--accent)`.

### Verification
- Executed `npm test` successfully (324 tests passed).

### Suggested Commit Message
`feat(ui): add visual active indicator to variant matching displayed cover`

Status: Completed
