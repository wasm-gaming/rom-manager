# Game Boy Advance Console Icon Redesign

Date: 2026-08-06 23:55

## Summary
Redesign the Game Boy Advance system logo in `src/components/SystemLogos.tsx` and the GBA controller icon in `src/components/icons.tsx` to match the classic Indigo Game Boy Advance (AGB-001) hardware reference provided by the user.

## Planned Changes

1. **`src/components/SystemLogos.tsx`**:
   - Update `case 'GBA':` vector SVG (`viewBox="0 0 128 128"`) to recreate the authentic Indigo GBA layout:
     - **Top Shoulder Trim / Bumpers**: Light silver/grey top bumper highlights (`fill="#d1d5db"`, `stroke="#9ca3af"`).
     - **Chassis Body**: Classic Indigo/Purple wide horizontal body (`fill="#544697"`, `stroke="#382e6e"`).
     - **Center Screen Assembly**:
       - Dark charcoal curved screen frame/bezel (`fill="#232328"`, `stroke="#17171a"`).
       - Inset rectangular display screen (`fill="#121316"` / `#1c1e24`).
     - **Left Controls**:
       - Light grey/silver D-pad cross (`fill="#e2e8f0"`, `stroke="#cbd5e1"`).
       - Two diagonal dark grey pill slots below D-pad for **SELECT** and **START** with light grey pin button heads.
     - **Right Controls**:
       - Light grey/silver circular **B** and **A** buttons positioned diagonally.
       - Speaker ventilation grille lines in bottom-right corner.

2. **`src/components/icons.tsx`**:
   - Separate `GBA` from generic handhelds in `Gamepad` function and add a dedicated `normalized === 'GBA'` block (`viewBox="0 0 24 24"`):
     - Indigo horizontal body (`fill="#544697"`).
     - Light grey top shoulder accents.
     - Dark screen bezel with LCD display inset.
     - Light grey D-pad on left, slanted A/B buttons on right, and start/select pills.

## Verification Plan

### Automated Tests
- Run `npm test` to ensure all existing tests pass without regressions.

### Manual Verification
- Verify the updated GBA logo in the ROM Manager UI (Welcome Carousel, System Library view, and System Badges) to ensure precise visual fidelity to the reference image.

## Walkthrough (2026-08-07 00:03)

### Accomplished Changes
- Redesigned `case 'GBA':` vector SVG in [SystemLogos.tsx](file:///Users/joshua/dev/wasm-gaming/rom-manager/src/components/SystemLogos.tsx) to portray the classic Indigo Game Boy Advance (AGB-001) chassis, including silver top shoulder bumpers, indigo body contouring, screen bezel with display, silver D-pad cross, angled SELECT/START slots, diagonal silver B & A buttons, and bottom-right speaker grille lines.
- Updated small inline Game Boy Advance system gamepad SVG in [icons.tsx](file:///Users/joshua/dev/wasm-gaming/rom-manager/src/components/icons.tsx) to mirror the horizontal GBA console profile.
- Verified all 357 unit tests pass cleanly.

### Suggested Commit Message
`style(icons): replace generic GBA icon with authentic Indigo console SVG`
