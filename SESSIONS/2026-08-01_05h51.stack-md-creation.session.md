# Session: STACK.md Creation

Date: 2026-08-01 05:51

## Objectives
Document the current technical stack, architecture, services, dataset pipelines, and applied functional/UI requirements in `STACK.md` in English as requested by the user.

## Planned Changes

### [NEW] STACK.md
Create `STACK.md` at the project root with the following structured sections:
1. **Overview**: Project identity, purpose, client-side zero-backend architecture.
2. **Technology Stack**:
   - UI & Framework: Preact, Preact Router, Preact Signals.
   - Language & Runtime: TypeScript, Node.js >= 24.0.0.
   - Build Tools: Vite (app dev & build), tsup (embeddable library build).
   - Hashing & Filesystem: `hash-wasm` (WASM CRC32/MD5/SHA1), File System Access API, `@cloudauthn/vfs-sync`.
   - Code Quality & Testing: oxlint, Vitest, TypeScript compiler (`tsc --noEmit`).
3. **Architecture & Project Structure**:
   - `src/core`: Pure domain logic (grouping, name normalization, covers, media types, regions, wizard tree).
   - `src/services`: Service layer for datasets, intake, scanning, organization, cover fetching, archive handling, storage, handles.
   - `src/components`: UI components (FileTree, SystemLogos, BrandLogo, WelcomeCardIllustrations, Tabs, icons).
   - `src/views`: Main view views (`ROMExplorer`).
   - `scripts/`: Dataset processing scripts (DAT fetch, cover fetch, JSON/SQLite generation, romset verification).
4. **Functional & Domain Requirements Applied**:
   - 100% Client-Side & Hash-Based Identification.
   - 24 MiSTer Systems & Medium Rules (Cartridge flat layout vs Disc hierarchical layout).
   - Romset checksum verification (Neo Geo / Zip dumps).
   - Metadata & Cache Layer (`.meta/`).
   - Title & Roman Numeral Normalization.
   - Flat vs. Wizard Explorer modes.
   - Non-destructive Plan -> Apply File Organization.
   - Intake flow & Game selection state preservation.
5. **UI / UX Features & Applied Specifications**:
   - Initial Landing View with 3-row infinite scroll marquee carousel (`.welcome-systems`), 2x2 cards with custom illustrations, and Legal/GitHub footer.
   - Reopen remembered directory with tab refresh action icon & persisted handles (`HandleStoreService`).
   - Split Tree Item container (left toggle/select, right inline actions).
   - Dynamic cover art aspect-ratio container with multi-region selection (EU, US, JP) and missing variant prevention.
   - Keyboard Navigation (Up/Down arrow key support for game/file selection).
   - Collection folder protection (curated user collections strictly unmanaged).

## Walkthrough
- Created `STACK.md` in English with complete technical stack and applied requirements specifications.

Suggested Commit Message:
`docs: create STACK.md with technical stack architecture and applied requirements`
