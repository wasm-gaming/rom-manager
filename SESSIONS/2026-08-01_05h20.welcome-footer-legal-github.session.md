# Welcome Landing Footer with Legal Info and GitHub Link

Date: 2026-08-01 05:20

## Summary
Add a footer at the bottom of the initial landing view (`WelcomePanel`) containing legal information (open-source disclaimer and trademark notice) and a GitHub repository link featuring the GitHub SVG icon.

## Planned Changes

### 1. Icons Component (`src/components/icons.tsx`)
- Add `GithubIcon` SVG component for rendering GitHub icon with `currentColor` fill.

### 2. Internationalization (`src/messages/es.yaml` & `src/messages/en.yaml`)
- Add `welcome.footer.legal` and `welcome.footer.github` translation strings in both Spanish and English.

### 3. Landing Component (`src/components/WelcomePanel.tsx`)
- Import `GithubIcon` from `./icons`.
- Append a `<footer>` element (`.welcome-footer`) containing the legal info text and the GitHub link with SVG icon.

### 4. Styles (`src/styles/index.css`)
- Add CSS rules for `.welcome-footer`, `.welcome-footer-content`, `.welcome-footer-legal`, and `.welcome-footer-github`.
- Ensure dark/light theme compatibility, hover effects, flex alignment, and proper spacing.

---

## Walkthrough

- Created `GithubIcon` SVG component in `src/components/icons.tsx`.
- Added translation keys `welcome.footer.legal` and `welcome.footer.github` to `src/messages/es.yaml` and `src/messages/en.yaml`.
- Integrated `<footer class="welcome-footer">` in `src/components/WelcomePanel.tsx` with legal disclaimer and GitHub link.
- Styled footer in `src/styles/index.css` with responsive flexbox layout, border separator, and hover styles.
- Verified with `npm run type-check`, `npm run lint`, and `npm run test` (348 tests passed).

### Suggested Commit Message
`feat(welcome): add landing footer with legal info and github link`

