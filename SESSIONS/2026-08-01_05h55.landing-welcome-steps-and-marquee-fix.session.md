# Session: Landing Page Welcome Steps Styling, Translation & Marquee Glitch Fix

**Date**: 2026-08-01 05:55
**Status**: In Progress

## Objectives
1. Update the translation key `welcome.how.title` in `src/messages/es.yaml` from `'Cómo va'` to `'Cómo funciona'`.
2. Enhance the styling of `.welcome-steps` in `src/styles/index.css` to feature modern card containers with hover effects, refined badges, and elevated visual hierarchy.
3. Fix the animation glitch in `.welcome-systems` infinite marquee by adjusting track item repetitions and spacing calculation for pixel-perfect loop transitions.

## Proposed Changes

### Translations
- Modify `src/messages/es.yaml`:
  - Change `welcome.how.title` to `'Cómo funciona'`.

### Styling (`src/styles/index.css`)
- Upgrade `.welcome-steps` and `.welcome-step`:
  - Turn `.welcome-step` into structured card containers matching `.welcome-card` design system.
  - Enhance `.welcome-step-number` with modern badge styling, border-radius, gradient/accent tint, and subtle shadow.
  - Add smooth hover transitions (`translateY(-2px)`, border highlight, shadow).
- Fix `.welcome-systems` marquee animation glitch:
  - Normalize item spacing so half-track transform (`translateX(-50%)`) aligns seamlessly without flex-gap off-by-one subpixel offsets.

### Components (`src/views/ROMExplorer/WelcomePanel.tsx`)
- Ensure 4x repetition for `topSystems`, `middleSystems`, and `bottomSystems` tracks so the marquee always spans well beyond double viewport width on high-res displays.

## Verification Plan
- Launch dev server (`npm run dev` or `vite build`) to verify clean build.
- Visually inspect the landing page in browser:
  - Verify section header displays "Cómo funciona".
  - Verify `.welcome-steps` are styled cleanly as interactive cards.
  - Verify `.welcome-systems` scrolling marquee loops infinitely with zero visual jump/glitch.

## Walkthrough
- Modified `src/messages/es.yaml` to update section title from `'Cómo va'` to `'Cómo funciona'`.
- Quadrupled track array repetitions in `src/views/ROMExplorer/WelcomePanel.tsx` to ensure long continuous tracks across all display widths.
- Styled `.welcome-steps` as modern card containers with hover elevation, accent border highlights, and styled step badges in `src/styles/index.css`.
- Fixed infinite marquee animation glitch in `src/styles/index.css` by replacing flex gaps with uniform item margins, guaranteeing that `translateX(-50%)` transitions seamlessly into `0%` with zero pixel jump.
- Ran `npx tsc --noEmit` cleanly without errors.

Suggested commit message:
`style(landing): update step section title, enhance welcome steps styling and fix marquee glitch`
